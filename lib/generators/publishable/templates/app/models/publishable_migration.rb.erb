class Create<%= name.classify %> < ActiveRecord::Migration
  def self.up
    create_table :<%= name.tablelize %> do |t|
      t.string      :title
      t.text        :body
      t.integer     :site_id
      t.integer     :position

      # t.string      :author_name
      # t.text        :extra_body
      # t.string      :kicker

      t.timestamps
    end

    Publinator::PublishableType.new(:name => "<%= name.classify %>", :sluggable => "true")
  end

  def self.down
    drop_table :<%= class_name.tableize %>
  end
end
