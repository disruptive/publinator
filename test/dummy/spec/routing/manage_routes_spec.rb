require 'spec_helper'

#module Publinator
#describe "publinator routing" do
  #it "Routes the root to the test controller's index action" do
    #{ :get => '/' }.should route_to(:controller => 'publinator/home', :action => 'index')
  #end

  #it "Routes /manage to publinator/manage#index action" do
    #{ :get => '/manage' }.should route_to(:controller => 'publinator/manage', :action => 'index')
  #end
#end

#describe "routes for viewing publishables" do
  #it "should route /articles to publishable#index" do
    #{ :get => "/articles/index" }.should route_to(:controller => "publishable", :action => "index")
  #end
#end

#describe "routes for managing widgets" do
  #it "should route /widgets to widgets#index" do
    #{ :get => "/widgets" }.should route_to(:controller => "widgets", :action => "index")
  #end
#end
#end
